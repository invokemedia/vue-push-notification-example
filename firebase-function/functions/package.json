{
  "name": "vue-push-notification-notify",
  "description": "Send notifications to all the users who subscribed to the website",
  "scripts": {
    "start": "NODE_ENV=local node index.js"
  },
  "dependencies": {
    "firebase-admin": "^4.2.1",
    "firebase-functions": "^0.5.1",
    "request": "^2.81.0",
    "request-promise": "^2.0.0"
  }
}
